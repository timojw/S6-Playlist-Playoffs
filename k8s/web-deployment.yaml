apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels:
    app: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: web
        image: timojw/web:1
        ports:
        - containerPort: 80
        - containerPort: 443
        env:
        - name: API_URL
          value: "http://4.182.194.187:8080"
